{
  "name": "ecommerce-click-streams-sink",
  "config": {
    "connector.class": "io.confluent.connect.hdfs3.Hdfs3SinkConnector",
    "tasks.max": "3",
    "topics": "ecommerce_transactions_streaming",
    "hdfs.url": "hdfs://localhost:9000/data/warehouse/ecommerce_transactions/streaming",
    "flush.size": "10",
    "confluent.topic.bootstrap.servers": "localhost:9092"
  }
}